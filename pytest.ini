[pytest]
# Only collect tests from the tests/ directory.
# Exclude scripts/ which contains diagnostic runner scripts (not pytest test files).
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
