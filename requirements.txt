# Legal Multimodal GraphRAG System — Python Dependencies
#
# Core (always required)
pydantic>=2.0.0

# PDF extraction (local-first)
pdfminer.six>=20221105

# DOCX extraction (.docx files — modern Word format)
python-docx>=0.8.11

# DOC extraction (.doc files — legacy Word format)
# docx2txt handles XML-based .doc files as plain text (no table topology).
# True OLE .doc files (pre-2007 Word binary format) require one of:
#   Option A — pywin32 + MS Word (Windows only, most reliable):
#     pip install pywin32
#   Option B — LibreOffice headless (cross-platform):
#     install LibreOffice and ensure `soffice` is on PATH
#   Option C — antiword CLI (Linux/macOS only):
#     macOS:  brew install antiword
#     Linux:  sudo apt install antiword
# The pipeline tries all options in order; first success wins.
docx2txt>=0.8
# pywin32>=306  # uncomment to enable Word COM automation on Windows

# HTML extraction
beautifulsoup4>=4.12.0
lxml>=4.9.0

# OCR (required for image and scanned PDF inputs)
# pytesseract requires Tesseract to be installed separately:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#             → During install, tick "Vietnamese" and "English" language packs
#   macOS:   brew install tesseract tesseract-lang
#   Linux:   sudo apt install tesseract-ocr tesseract-ocr-vie tesseract-ocr-eng
pytesseract>=0.3.10
Pillow>=10.0.0

# Optional: PDF page rendering for layout analysis
# pdf2image>=1.16.0
# poppler-utils must also be installed on the OS

# Optional: AI-assisted repair (not used by default)
# openai>=1.0.0
# anthropic>=0.20.0

# Phase 10: Product Runtime API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.24.0          # required by FastAPI TestClient

# Development / testing
pytest>=7.4.0
